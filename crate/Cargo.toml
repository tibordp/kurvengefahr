[package]
name = "kg_toolpath"
version = "0.1.0"
edition = "2021"
description = "Toolpath generation for Kurvengefahr (pen-plotter CAM)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
